#include <stdio.h>

int main()
{
    int days;
    float pi;
    printf("How many days in the week? ");
    scanf("%d",&days);
    printf("The value of Pi to two points: ",pi);
    scanf("%f",&pi);
    printf("There are %d days in the week.\n",days);
    printf("Pi value is %f.\n",pi);
    return 0;
}
